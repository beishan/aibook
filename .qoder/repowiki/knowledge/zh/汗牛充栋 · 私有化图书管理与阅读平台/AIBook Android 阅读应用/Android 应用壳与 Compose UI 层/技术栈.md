Android Application (Kotlin) + Jetpack Compose (BOM 2025.01.01) + Navigation Compose 2.8.7 + Lifecycle ViewModel Compose；EPUB 阅读使用 `org.readium.kotlin-toolkit:readium-shared:3.1.0` 与 `readium-streamer:3.1.0`；图片加载 Coil 3.1.0；OPDS 解析 JSoup 1.18.1；HTTP OkHttp 通过自定义 `OkHttpOpdsTransport` 注入。