构建使用 Maven：`mvn clean package -DskipTests` 产出可执行 jar；运行需设置环境变量 `SPRING_DATASOURCE_URL/USERNAME/PASSWORD`、`REDIS_HOST/PORT/PASSWORD`、`JWT_SECRET`、`MINIO_ENDPOINT/ACCESS_KEY/SECRET_KEY`、`UPLOAD_PATH`；默认端口 8080，上传大小上限 500MB。