# Task A — Room 12→13 migration instrumentation coverage

Status: **DONE_WITH_CONCERNS**

## Scope completed

- Added an Android instrumentation migration test using `MigrationTestHelper`.
- The test creates a version-12 database, inserts a populated existing PDF book row, runs migration 12→13, and verifies:
  - the existing row and representative reading progress values are preserved;
  - the new `progressPdfZoom` column exists;
  - `progressPdfZoom` is `NULL` for the pre-existing row.
- Added the AndroidX test and Room testing dependencies required by the test.
- Enabled Room schema export, exposed the schema directory as AndroidTest assets, and checked in version 12 and 13 schema JSON files.
- Exposed only `MIGRATION_12_13` so the migration test executes the same migration registered by production.

## Files changed

- `core/data/build.gradle.kts`
- `core/data/src/main/kotlin/com/aibook/android/core/data/db/AiBookDatabase.kt`
- `core/data/src/androidTest/kotlin/com/aibook/android/core/data/db/AiBookDatabaseMigrationTest.kt`
- `core/data/schemas/com.aibook.android.core.data.db.AiBookDatabase/12.json`
- `core/data/schemas/com.aibook.android.core.data.db.AiBookDatabase/13.json`

No files were staged or committed.

## TDD evidence

### RED

Command:

```text
./gradlew :core:data:compileDebugAndroidTestKotlin
```

Expected failure was observed after adding the test first:

```text
AiBookDatabaseMigrationTest.kt:39:28 Cannot access 'val MIGRATION_12_13: Any': it is private in 'AiBookDatabase.Companion'.
AiBookDatabaseMigrationTest.kt:39:13 Argument type mismatch: actual type is 'Any', but 'Migration' was expected.
BUILD FAILED
```

The first sandboxed attempt could not access the existing Gradle user cache; the command was immediately rerun with approved Gradle cache access. The RED above is from that actual compiler run. The same run also exposed missing Kotlin-test assertions, which were minimally changed to JUnit assertions already available to Android instrumentation tests.

### GREEN compile

Command:

```text
./gradlew :core:data:kspDebugKotlin :core:data:compileDebugAndroidTestKotlin
```

Result:

```text
> Task :core:data:compileDebugAndroidTestKotlin
BUILD SUCCESSFUL in 5s
```

### GREEN instrumentation APK assembly

Command:

```text
python3 -m json.tool core/data/schemas/com.aibook.android.core.data.db.AiBookDatabase/12.json >/dev/null
python3 -m json.tool core/data/schemas/com.aibook.android.core.data.db.AiBookDatabase/13.json >/dev/null
./gradlew :core:data:assembleDebugAndroidTest
```

Result:

```text
> Task :core:data:assembleDebugAndroidTest
BUILD SUCCESSFUL in 18s
```

The resulting instrumentation APK contains both assets:

```text
assets/com.aibook.android.core.data.db.AiBookDatabase/12.json
assets/com.aibook.android.core.data.db.AiBookDatabase/13.json
```

## Concerns / remaining runtime verification

- No Android device or emulator is connected, so the instrumentation test was compiled and packaged but was **not executed**. No connected-test pass is claimed.
- Version 13 schema was generated by Room KSP. Version 12 was regenerated by Room KSP from detached commit `9ee10ff`, whose database version is 12; it is accurate to that source state but was not archived during the original v12 build.
- `core/data/build.gradle.kts` and `AiBookDatabase.kt` already contained unrelated dirty feature changes before this task; they were preserved. The diff for this task is limited to AndroidTest/schema configuration, dependencies, schema export, and migration visibility.
