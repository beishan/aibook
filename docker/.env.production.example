# 将此文件复制为 Jenkins 的 Secret file 凭据，建议凭据 ID：aibook-production-env。
# 不要把包含真实密码的 docker/.env.production 提交到 Git。

DB_PASSWORD=请替换为强数据库密码
REDIS_PASSWORD=请替换为强Redis密码
MINIO_USER=aibook
MINIO_PASSWORD=请替换为强MinIO密码
JWT_SECRET=请替换为至少32字节的随机JWT密钥

# 必须是飞牛 NAS 宿主机上的绝对路径，不是 Jenkins 容器内路径。
BOOKS_PATH=/vol1/docker/aibook/books
# 书库目录授权用户组的数字 GID，可用 `stat -c '%g' "$BOOKS_PATH"` 查询。
BOOKS_GID=1001
# 可选附加书库。环境文件中多个挂载用分号分隔；Jenkins 参数支持每行一个。
# 示例：/vol1/media/novels:/scanfolder/novels:ro;/vol2/media/comics:/scanfolder/comics:ro
BOOKS_MOUNTS=
# 附加书库需要加入容器的数字 GID，多个值用逗号分隔。
BOOKS_GIDS=

# 可按 NAS 上的端口占用情况修改。
FRONTEND_PORT=8291
BACKEND_PORT=8292
MINIO_API_PORT=9200
MINIO_CONSOLE_PORT=9201

JAVA_OPTS=-Xms256m -Xmx512m
APP_LOG_LEVEL=INFO
SECURITY_LOG_LEVEL=INFO
DOCKER_LOG_MAX_SIZE=10m
DOCKER_LOG_MAX_FILE=3
